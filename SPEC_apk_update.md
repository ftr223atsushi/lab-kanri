# APK の更新配布（v2.82 で実装）

端末をUSBで繋がなくても、現場で自分で更新できるようにする仕組み。

---

## しくみ

1. アプリは起動3秒後に GAS へ `ping` する
2. GAS は `APP_VERSION` と `LATEST_APK_URL` を返す
3. **GAS の版が自分より新しく、URLが入っていれば**、画面下にバナーを出す
4. 「⬇ 更新」を押すと外部ブラウザが開いてAPKをダウンロード
5. ダウンロードしたファイルをタップして上書きインストール

版の比較は `2.82-20260912.2114` の先頭の数（2.82）だけを見る。
URLが空（既定）なら何も出ない。圏外でも黙って何もしない。

---

## 最初の1回だけやること

### ① ドライブに置き場を作る

`shast-LAB2-latest.apk` のような名前で**1つだけ**ファイルを置く。
以後のリリースは、このファイルを**「版を管理」で差し替える**（新規アップロードしない）。
そうすればファイルIDが変わらないので、URLを書き換えずに済む。

共有設定は「リンクを知っている全員 = 閲覧者」にする。
（端末に Google アカウントが無くても落とせるようにするため）

### ② URL を作って Code_v2.gs に書く

ドライブの共有リンクは
`https://drive.google.com/file/d/【ファイルID】/view?usp=sharing`

この **ファイルID** を使って、次の形に直す。

```
https://drive.usercontent.google.com/download?id=【ファイルID】&export=download&confirm=t
```

`Code_v2.gs` の `LATEST_APK_URL` にこれを書く。**1回書けば以後変えない。**

**設定済み (2026-09-12)** — ファイルID `1nRTD6j2VnaSKf--TaQq7ZBbT80J1K9td`
（ドライブ上の名前は版入り。2026-09-22 時点 `shast-LAB2-3.02.apk`）

> `drive.google.com` のままだと、端末のドライブアプリが横取りして
> ダウンロードにならない。shast のときに引っかかった罠。

### ③ 端末側の許可

初回だけ、ブラウザに「不明なアプリのインストール」を許可する必要がある。
ダウンロードしたAPKをタップすると Android が聞いてくるので、そこで許可する。

---

## リリースのたびにやること

1. APK をビルドし、`shast-LAB2-2.89.apk` のように**版入りの名前**で
   **`C:\app_dev\lab-kanri\配布\`** に置く（置き場はここで固定。git 管理外）。
   置いたら Claude がエクスプローラーでそのファイルを選択した状態で開くので、
   そのままドライブの画面へドラッグすればよい
   （デスクトップに置いていた時期は毎回どこにあるか分からなくなった）
2. ドライブのファイルを**差し替え**（版を管理 → 新しい版をアップロード）。
   **差し替えたら表示名を `shast-LAB2-3.02.apk` のように版入りに変える**（2026-09-22 ユーザー指示）。
   名前を変えてもファイルIDは変わらないので、`LATEST_APK_URL` はそのまま。
   端末に落ちるファイル名も版入りになるので、ダウンロード一覧で見分けられる。
   UM790 からは rclone で同名上書き→リネームができる（IDが保たれる）:
   ```
   rclone copyto --drive-root-folder-id 14FOkg9Upqpk_Frf4uCUyZvE7OGz61m8Q <apk> gdrive:shast-LAB2-<旧版>.apk
   rclone moveto --drive-root-folder-id 14FOkg9Upqpk_Frf4uCUyZvE7OGz61m8Q gdrive:shast-LAB2-<旧版>.apk gdrive:shast-LAB2-<新版>.apk
   ```
   ※ copyto の宛先は**いまドライブにある名前**にする（別名だと新規ファイルになりIDが変わる）
3. `Code_v2.gs` の `APP_VERSION` を上げて貼り付け → **デプロイを管理 → 新バージョン**
4. 端末がアプリを開けばバナーが出る

**②→③の順を守る。** 先にGASを上げると、端末が落としてくるAPKが古いままなので
バナーが永久に消えなくなる。

版を管理で差し替えると、ドライブに残る「実体」は常に最新の1つだけ。
表示名は版入り（`shast-LAB2-3.02.apk`）にしておく。**最終的にどの版が入っているかは
アプリ画面右上の表記で確かめる。**

APK側の `index.html` のバージョン表記も同じ文字列に揃えること（従来どおり）。

---

## 注意

- **署名鍵が変わると上書きできない。** いまは
  `C:\Users\user\.android\debug.keystore`（バックアップ済み）。
  別のPCでビルドすると鍵が変わり、アンインストールが必要になる
- バナーは「閉じる」で消せるが、アプリを開き直すとまた出る（更新するまで）
- 更新を強制する仕組みは入れていない。必要なら最低版のチェックを足せる
