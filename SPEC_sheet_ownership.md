# SPEC: シート所有権の分離（1シート = 1書き手）

作成: 2026-08-23（lab-kanri セッション発、3プロジェクト共通仕様の草案）
状態: **picker側 実装済み（2026-08-31・GAS v2.10、picker本番リリース待ち）** ／ lab-kanri・shast側は未実施

## 0. picker実装時の決定事項（2026-08-31追記）

1. **「ラボ記録」シートは picker が新規作成時にヘッダーだけ生成する**（§3の表では lab-kanri 生成としていたが変更）。
   理由: (a) 分析検体シートの揃いCOUNTIFSが「ラボ記録」を参照するため、シートが無いと#REFになる
   (b) lab-kanri の新旧判別（§4-4「ラボ記録の有無」）が最初のスキャン前から正しく効く。
   **行の書き手は lab-kanri のみ**（find-or-append）＝1シート1書き手の原則は維持。
   列は§3.2のとおり15列（コード〜備考）。日時列(D/F/H/J/L)は書式設定済み。
2. **未決#2（揃いCOUNTIFSのキー）**: 表層・配管＝地点キー（B列）＋コードの種別パターンで絞る
   （ラボ記録は全種別が同居するため。表層 `A:A="*-S-*"` を2行、配管 `"*-H-*"` を1行）。
   深度・地下水＝コードキー（A列）を1行。picker の「種別」列(C)には依存しない
   （lab-kanri は C列を自由な表記で転記してよい）。
3. **未決#3（ガスの分析記録）**: 統一案を採用。新構成の土壌ガスシートは採取担当まで
   （受入・分析列なし）＝shast専有。ガスの分析記録もラボ記録へ。
4. **新旧判別（picker/GAS側）**: 実データシートのヘッダーに「受入日時」が有れば旧構成ブックと
   判定し、マージ・派生シートの数式再生成とも旧仕様のまま行う（既存V2ブックは無改造で完走）。
5. **V1→V2移行**は旧構成ブックを生成する（V1の受入/風乾記録をそのままコピーするため）。

---

## 1. 背景・課題

- v2ブックの実データシート（表層土壌等）には **picker / shast / lab-kanri の3者が書く**。
  このため誰かが列を追加すると他の2者（固定列実装）が壊れる。
- shast は見出し式（headers-g v2.81）で列ズレ耐性を獲得済み。**picker と lab-kanri は固定列のまま**。
- 今後も列追記の需要は増える（shast の野帳シート化構想、被覆・検測、その他）。
- lab-kanri v2 には「振り/ろか/分析の書込先が仕様に無い」という未解決の穴があり、
  暫定で「前処理」シートを自動作成している。本仕様はこの穴の正式解決も兼ねる。

## 2. 原則

1. **1シート = 1書き手**。シートに書き込むアプリは1つだけにする。読むのは誰でも自由。
2. 行の対応付けは**コード**（不変キー）で行う。
3. 書き手が1アプリだけのシートは、そのアプリがレイアウトを保証できるので
   **固定列のままで安全**になる。→ picker / lab-kanri の見出し式化工事は不要になる。
4. 読み取りは壊れても書込事故にならないので、読み取り側の列参照は従来方式でよい
   （壊れたら直せばよい。書込のような取り返しのつかない事故がない）。

## 3. シート構成（新規現場から適用）

| シート | 作成 | 書き手 | 内容 |
|---|---|---|---|
| 実データシート4枚（表層土壌 / 土壌ガス / 配管・ピット・盛り土下 / 深度調査） | picker | **shast のみ** | 現場作業の記録（採取・削孔・被覆・検測・現地深度実測） |
| **ラボ記録**（新設・1枚） | **lab-kanri が自動生成** | **lab-kanri のみ** | ラボ工程の記録（受入〜分析） |
| 分析検体ほか派生シート | picker | 数式のみ（印刷日は人） | 揃い状況の参照先を「ラボ記録」に変更 |
| 進捗まとめ（任意・表示用） | picker or 手動 | 誰も書かない | QUERY/VLOOKUP で実データ+ラボ記録をコード合成。人間はここを見る |

### 3.1 実データシートの列（ラボ列を削除）

現行v2からの変更 = **受入以降のラボ列を削除**（shast の列だけ残す）:

```
表層土壌:   状態｜地点｜上下｜コード｜採取日時｜採取担当｜被覆｜検測深度
土壌ガス:   状態｜地点名｜コード｜削孔日時｜削孔担当｜採取日時｜採取担当
配管等:     状態｜地点｜採取深度｜コード｜採取日時｜採取担当｜現地深度｜被覆｜検測
深度調査:   状態｜地点｜深度｜コード｜採取日時｜採取担当｜被覆｜検測
```

※ shast は見出し式なので列が詰まっても追従できる。

### 3.2 「ラボ記録」シートの列（lab-kanri が生成・専有）

```
コード｜地点｜種別｜受入日時｜受入担当｜風乾日時｜風乾担当｜振り日時｜振り担当｜ろか日時｜ろか担当｜分析日時｜分析担当｜ラボ深度｜備考
```

- 行は**スキャン時にコードキーで find-or-append**（picker が行を用意する必要なし）
- 1コード = 1行。フェーズ1コード（S/G/H/D/WG/WP）も Lコード（振り/ろか）も同じシートに同居
  （コードが別なので混ざらない。Lコード行は受入〜風乾が空のまま）
- 地点・種別は実データシート/分析検体シートから転記する**表示用参考列**（正本はコード）
- 順番チェック・二重チェックはこのシート内で完結
- 現行の暫定「前処理」シートはこのシートに統合して廃止

### 3.3 現地深度・水位の正本

- 実データシートの「現地深度」= **shast の実測が正本**（lab-kanri は書かない）
- ラボで計測・補完した深度/水位は「ラボ記録」の**ラボ深度**列へ
- 表示上どちらを採るかは、まとめシートの数式で「実データ優先・空ならラボ」とする

## 4. 各プロジェクトへの依頼事項

### picker（picker セッションで実施）
1. 実データシート生成の列構成変更（§3.1、ラボ列削除）
2. 分析検体シートの「検体揃い状況」COUNTIFS の参照先を「ラボ記録」の受入列へ変更
3. （任意）進捗まとめシートの生成

### shast（shast セッションで実施）
- ほぼ変更なし（自分の列は不変。見出し式なので列詰めにも追従）
- 確認のみ: 実データシートからラボ列が消えても読み取りで参照していないか

### lab-kanri（このセッションで実施）
1. 「ラボ記録」シートの自動生成 + find-or-append 書込（handlePhase1V2 / handlePhase2V2 の書込先変更）
2. 実データシートは**読み取り専用**に（コード→地点/種別/状態(削除判定)の解決のみ）
3. ビュー（検体/採取/4種チップ）: 採取系=実データシート読み、受入以降=ラボ記録読みに変更
4. 新旧構造の自動判別: **「ラボ記録」シートの有無**で分岐
   （現行構造ブック=BQYR等は従来どおり実データシートに書く。移行不要）

## 5. 適用範囲・移行

- **新規現場から適用**。進行中の現場（BQYR 等）は現行構造のまま完走
- lab-kanri はシート有無判別で新旧どちらのブックでも動く（§4-4）
- 旧方式（地点抽出ベース）の現場は旧アプリ shast LAB のまま（本仕様の対象外）

## 6. 未決事項

| # | 項目 | 案 |
|---|---|---|
| 1 | ラボ記録シートの名前 | 「ラボ記録」（仮）。「lab記録」「受入記録」等でも可 |
| 2 | 揃いCOUNTIFSの具体式 | picker 実装時に決定（コード or 地点キーか） |
| 3 | ガスの分析記録の置き場 | ラボ記録に統一する案を推奨（現行はガスシートの分析列。統一すればガスシートも shast 専有になる） |
| 4 | 適用開始時期 | picker v2 の本番リリースに同期が理想 |
