# ラボ記録まわり 再整理（2026-09-09 夕方）

**前半で決めた「ラボ記録タブを新設する」案は取り下げ。** 下が現時点の結論。
実装は未着手。深度調査は保留。

---

## 0. 方針の転換

分析検体タブは picker と lab-kanri で列を分けて使うしかない。
つまり**シート単位で書き手を分けるのは、どのみち成立しない**。
ならば新しいタブを増やさず、**列で書き手を分ける**方が単純になる。

| 旧案 | 新案 |
|---|---|
| 1シート1書き手（ラボ記録タブを新設） | **1列1書き手**（既存タブに列を足す） |
| コードを2か所に持つ | コードは1か所のまま |
| 地点変更でミラーの検討が要る | 検討不要 |

元々の問題は「採取列を shast と lab-kanri の両方が書く」ことだった。
**現地確認の列を別に作れば、それだけで解決する。**

---

## 1. どこに何を書くか

### 1.1 実データタブ（表層土壌 / 土壌ガス / 配管） ← 現地確認・受入・風乾

picker が **現地確認日時 / 現地確認担当** の2列を追加する。

| 列 | 書き手 |
|---|---|
| 状態 / 地点名 / コード | picker |
| 採取日時 / 採取担当 | **shast**（作業した事実） |
| **現地確認日時 / 現地確認担当** | **lab-kanri**（その検体を現地で確認した） |
| 受入日時 / 受入担当 | lab-kanri |
| 風乾日時 / 風乾担当 | lab-kanri |
| 削孔日時 / 削孔担当（ガスのみ） | shast |
| 分析日時 / 分析担当（ガスのみ） | lab-kanri |

- 混合前の工程なので、**1地点＝1行**で足りる
- ガスはLコードを持たず、**Gコードを最後まで使う**

### 1.2 分析検体タブ（振り・ろか・分析）

混合すると `H5 (2,4①,5①,6①,8①)` のように複数地点が1検体になる。
地点単位の実データタブには収まらないので、**分析検体タブの行がその検体**。

picker が **ろかコードの右側**に6列追加する（見出し名は要確定）。

```
振り日時 / 振り担当 / ろか日時 / ろか担当 / 分析日時 / 分析担当
```

- 行は既にあるので lab-kanri は**追記しない**。ろかコードで照合して書くだけ
- picker はこの6列を**書かない・消さない**

---

## 2. 振りコードを廃止（会社決定）

**ろかコード1本をラボ内で使い回す。容器が変わるときはシールを貼り替える。**
振りとろかの2枚を作って地点名を照合する仕組みはやめる。

### picker
- 分析検体タブの **振りコード列（今のQ列）を削除**。ろかコードだけ残す

### lab-kanri（改修が要る）
- **接尾辞で工程を自動判定するのをやめる**（`SUFFIX_TO_MODE`）
  今は `-roka` を読むと必ず「ろか」に切り替わるので、**このままだと振りが記録できない**
  → 工程は他と同じく**モードボタンで決める**
- `MODES` の `expectedSuffix` チェックを外す（`-roka` だけを想定）
- 「Lコードは -huri / -roka 付きで読んでください」のエラー文を見直す
- 揃いラベル（機能B）から**振りの選択肢を削除**。ろかのみ

---

## 3. 分析検体閲覧ビュー：ガスを1地点1行にする

いまガスは `buildAreaMapFromRows` で**区画にまとめて**表示している（A1に2地点あれば1行）。
ラベルはコード1つにつき1枚なので、**まとめるとコードが出せない**。

→ ガスは区画でまとめず、**1地点1行**にして、その行にGコードを持たせる。

あわせて、いまガスの新着揃いは `color: ''` かつ**コードを持っていない**ため、
機能Bでガスのラベルが1枚も作れない状態。ここも同時に直る。

---

## 4. 前提になる改修：実データタブを見出し式にする ★

`KIND_CONFIG.workCols` が**列番号の決め打ち**になっている。

```
'表層土壌': { STATUS:1, POINT:2, UD:3, CODE:4, SAISHU:5, ... }
```

このままだと **現地確認の2列を足した瞬間に全部ずれる**。
分析検体タブは v2.58 で見出し式にしたので、実データタブも同じにする。

- 見出し名で列を引く（`getKentaiCols_` と同じやり方）
- 見つからない見出しがあれば、黙って別の列に書かずエラーで止める

**これを先にやらないと、picker が列を足した瞬間に事故る。**

---

## 5. 使わなくなるもの

- **ラボ記録タブ**（新設しない）。`SHEET_LABREC` まわりの分離構成コードは出番が無くなる
- **前処理シート**（振り/ろか/分析の暫定置き場）。分析検体タブに移すので廃止
- 振りコード

---

## 6. 未確定

1. 実データタブで現地確認の2列を**どこに入れるか**（見出し式にすれば位置は自由）
2. 分析検体タブに足す6列の**見出し名**
3. ガスの分析日時は実データタブでよいか（Gコードなのでそうなるはず）
4. 深度調査の扱い
