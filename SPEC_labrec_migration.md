# ラボ記録への移行 メモ

2026-09-09 の打ち合わせで決まったこと。**未着手。** picker 側でラボ記録タブができてから進める。

関連: `SPEC_sheet_ownership.md` §3.2 ／ `Code_v2.gs` の `SHEET_LABREC` `LABREC_H` `LABREC_MODE`

---

## 1. 採取と現地確認の考え方（ユーザー説明・2026-09-09）

**shast が工種タブに書く「採取日時」は、作業した事実の記録であって、検体を採った日時ではない。**
そのため、検体そのものを現地で確認した記録が別に要る。それが lab-kanri の「現地確認」。

| 記録 | 書き手 | 置き場 |
|---|---|---|
| 採取日時（作業した事実） | shast | 工種タブ（表層土壌など） |
| 現地確認日時（その検体を現地で確認した） | lab-kanri | ラボ記録 |

これで1シート1書き手になり、採取列を2つのアプリが取り合う今の状態が解消する。

## 2. picker 側で作るシート

シート名は **`ラボ記録`**（「ラボ」ではない）。見出しは**一字一句この通り**。順番は自由。

```
コード / 地点 / 種別 / ラボ深度
現地確認日時 / 現地確認担当
受入日時 / 受入担当
風乾日時 / 風乾担当
振り日時 / 振り担当
ろか日時 / ろか担当
分析日時 / 分析担当
```

- **コード列は必須**。lab-kanri はコードで行を探して、無ければ追記する
- 1つでも欠けると `getLabRecCols_` が例外で止まる（黙って別の列に書く事故を防ぐため）
- `ラボ記録` があれば lab-kanri は自動で分離構成として動き、工種タブには書かなくなる

## 3. lab-kanri 側でやること（未着手）

### ① 工程モードのボタン名を「採取」→「現地確認」に変える
書き込み先もメッセージも既に現地確認なので、**ボタンの文字だけ食い違っている**。

注意: 現行構成（ラボ記録なし）の現場では、いまも工種タブの採取列に書く。
そちらでボタン名だけ変わると意味がずれる。
→ **分離構成のときだけ「現地確認」と表示する**のが安全。
   `getSpreadsheetMeta` が `structure` を返しているので、それで出し分けられる。

### ② ガスの工程から「削孔」と「採取」を外す
現場で行う記録は shast が持つため、lab-kanri には不要。

### ③ ガスの現地確認をどこに書くか **← 未決**
- 案A: ラボ記録に入れる（土壌と同じ扱い。1シート1書き手が保てる）
- 案B: 土壌ガスタブに入れる（ガスだけ別扱いになる）

ガスは受入までしか工程が無く、ラボ工程（風乾・振り・ろか）を持たない。
ラボ記録に混ぜると空欄の多い行が増える一方、置き場が1つで済む。
**次回ここから。**
