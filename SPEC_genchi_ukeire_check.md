# 現地確認ビューの受入照合 ＋ 分析項目の表示（設計メモ・未実装）

2026-09-16 整理。ビルドはまだ。実装前にもう一度読み直すこと。

---

## 経緯（なぜこの形になったか）

- 最初は「現地確認ビューを分析検体ビューに統合する」案だった → **取り下げ**
- 理由1: 現地確認・受入は**地点ごと**の記録。分析検体は**混合検体ごと**に1行なので、
  地点単位の情報を行に並べられない
- 理由2: 分析検体は同じ地点が **黒・赤・青で3行**になる。地点の情報を置くと3重に出る
- 結論: **地点単位の話は現地確認ビュー、検体単位の話は分析検体ビュー**で完結させる

ラボが見たいのは「**現地で確認済みなのに受入が無い ＝ 受入忘れ**」。
現場の人は日報の件数だけ見ればよく、揃いは意識しない。

---

## A. 現地確認ビューに受入を重ねる（受入忘れの発見）

### 今ある材料（GAS変更なし）

`getSaishuAll` → `readKindWorkRows_` が返す行には既に両方入っている。

| index | 内容 |
|---|---|
| 7 / 8 | 受入日時 / 受入担当 |
| 10 / 11 | 現地確認日時 / 現地確認担当 |

現地確認ビュー（`renderSaishuKind` → `buildAreaMapFromRows(rows,'saishu')`）は
いま index 10 だけ見ている。index 7 を一緒に持たせるだけでよい。

### セルの見せ方（区画 × 1〜9）

1マスの状態を3段階にする。表層は上下で2つ、他は1つ。

| 状態 | 現地確認 | 受入 | 見え方 |
|---|---|---|---|
| 未 | なし | なし | 今の空欄のまま |
| **現地済・受入未** | あり | なし | **目立つ色（橙）**。これが受入忘れ候補 |
| 受入済 | あり | あり | 今の ✓（緑） |
| 現地なし・受入あり | なし | あり | 薄い印（現地確認を飛ばした検体。任意） |

- 表層の ✓×2 / n/m の表記（`itemMark`）はそのまま。色だけ受入で決める
- ステータス行に「**現地済・受入未 n件**」を足す。0件なら出さない
- 「現地済・受入未だけ表示」のチップを1つ足すと探すのが速い（任意）

### セルタップの小窓（`showSaishuDetail`）

今は現地確認の行だけ。受入の行を足す。

```
🌱 A1-3 現地確認 (表層)
上  現地確認 09/14 10:12 田中   受入 09/14 15:40 石徹白
下  現地確認 09/14 10:12 田中   受入 ―― 未受入   ← ここが橙
```

`buildDetailHtml` の `fmtDetailLine` を2回呼ぶだけで済む見込み。

### やらないこと

- 分析検体ビューには手を入れない
- シートには何も書かない（照合は表示だけ）

---

## B. 分析検体ビューで分析項目を出す（区画名タップ）

### 要望

`A1（24568）赤` をタップ → 「水銀　鉛」のように、その検体で分析する項目を出す。
ガスは `A1-1` をタップ → 「ベンゼン　トリクロロエチレン」など。

### 項目はどこにあるか（調査結果）

分析検体シートの 黒/赤/青 列には **色の印しか入っていない**（項目名は無い）。
項目名は picker が同じブックに書く **マトリクスシート**にある。

**土壌分析項目** シート（picker `buildSpecSheetsData` が生成）

```
No | 地点            | 溶出 |      |     | 含有 |      |
 1 | A1 (2,4,5,6,8)  | カドミ| 鉛   | 水銀 | 鉛   | 六価 |
```

- 1行目は「溶出」「含有」の見出しが左端セルにだけ入り、右は空セル（結合）
- **項目名は見出しではなくデータセルに入る**（該当すれば略称、しなければ空）
- 略称: カドミウム→カドミ、六価クロム→六価、アルキル水銀→アルキル（`ABBR`）
- 最終行は「検体数」の集計行 → 読み飛ばす
- **地点列の文字列 ＝ 分析検体シートの A列（区画）と同じ文字列**（同じ `g.loc` から出力）
  → これが結合キー

**第1種分析項目** シート（ガス）

```
地点  | 分析     | ベンゼン | トリクロロ… |
A1-1  | 土壌ガス | ベンゼン |            |
```

- こちらは見出しに項目名がある。データセルは該当なら略称、なければ空
- 結合キー ＝ 地点名（`A1-1`）

**深度・分析項目** シートは別構造（深度ごと）。今回は対象外。

### 色との対応

picker の項目定義（`analyses: { '溶出': '黒', '含有': '赤' }` など）から、規則は次の3つだけ。

| 分析検体の色 | マトリクスのどこを見るか |
|---|---|
| 黒 | 溶出グループの列すべて |
| 赤 | 含有グループの列のうち **六価・シアン以外** |
| 青 | 含有グループの列のうち **六価・シアン** |

青は「六価クロム」「シアン」の2項目で固定（土対法の定義上増えない）。
lab-kanri 側に定数として持ってよい。

### 実装の形（提案）

**GAS**: `getKentaiData` の返り値に `items` を足す。

```
items: {
  soil: { 'A1 (2,4,5,6,8)': { 溶出: ['カドミ','鉛','水銀'], 含有: ['鉛','六価'] }, ... },
  gas:  { 'A1-1': ['ベンゼン', 'トリクロロエチレン'], ... }
}
```

- 土壌分析項目シートを読み、1行目の「溶出」「含有」の位置で列を2グループに分ける
- 各データ行の 地点列をキーに、空でないセルを集める。最終行（検体数）は捨てる
- シートが無い現場（旧ブック）は `items` を空で返す。アプリは黙って何も出さない

**アプリ**: 区画セル（ガスは地点名）にタップを付け、`detailPopup` に出す。

```
🧪 A1 (2,4,5,6,8)  赤
含有: 鉛　六価 → 青なので除く → 「鉛」
```

表示は略称のまま（シートと同じ文字なので突き合わせやすい）。

### picker に頼むこと

**無し。** 今の出力で足りる。
ただし将来 picker が略称や見出し文字を変えると読めなくなるので、
実装したら picker 側の `SPEC_code_ledger.md` に「lab-kanri がこのシートを読む」と一行足してもらう。

---

## 進め方

1. A を先にやる（GAS変更なし・アプリだけ・当日の受入忘れ発見に直結）
2. B は GAS＋アプリ。A が落ち着いてから
3. どちらも仕様のとおりで良ければ、着手前に一言もらう

## 決めてほしいこと

- A: 「現地なし・受入あり」も印を付けるか（現地確認を飛ばした検体の発見になる）
- A: 「現地済・受入未だけ表示」チップを付けるか
- B: 表示は略称のままでよいか（カドミ／六価／アルキル）
