# ラベル印刷 仕様（機能A / 機能B）

作成: 2026-09-09　対象: shast LAB2 (lab-kanri2)　状態: **仕様確定・未実装**

日報の印刷は今まで通り。ここで足すのは「ラベル印刷」2種類。

---

## 0. 前提（調査済み）

| 項目 | 状態 |
|---|---|
| 日報印刷 | v2.54 で Android 印刷ブリッジ経由に修正済み。**維持** |
| 「▢ コード」バーコード印刷 | **LAB2 では動かない** → 機能B で置き換える |
| QR 生成 | 外部API依存をやめ、**ライブラリを assets に同梱** |

### 「▢ コード」が動かない理由（3つ重なっている）

1. `window.open('', '_blank')` で新タブを開くが、この WebView は複数ウィンドウを許可していない
2. GAS 側 `getBarcodePrintData` / `markBarcodePrinted` が **Code_v2.gs に未移植**（旧 Code.gs のみ）
3. コード形式が旧式 `A1-1-R-roka` のまま。picker v2 は `BQYR-L-0006-roka`

---

## 1. 現場名（機能A・B 共通）

件名シートには**現場IDしか無く、現場名が無い**。ブック名は長すぎる。

→ **印刷画面で現場名を手入力する。入力した値は記憶して、次回から初期値として出す。**

- 保存先は GAS のスクリプトプロパティ（現場のブックIDをキーにする）
  - 件名シートは picker が書くシートなので触らない（所有権の原則）
  - 一度入れれば **4台の端末すべてで同じ名前が出る**
- 毎回その場で直せる（記憶した値を上書きできる）
- **機能A** はそのまま出す（例 `畜産研究所`）
- **機能B** は幅 25mm しかないので**先頭4文字に切る**（例 `畜産研究`）

---

## 2. 機能A：受入ラベル（1検体 ＝ A4 1枚）

### 用途
**A4 を机に並べ、その紙の上に地点の土を置く。** 何の土か分かるようにするための名札。

### 入口
「🖨 本日」→ **受入の行だけ**に絞って一覧表示し、各行にチェックボックス。**既定は全部ON。**

### 出力
- A4 縦。**用紙のいちばん上**に1行。以下は無地（土を置く場所）
- 文字の上端が用紙上端から約 10mm。上寄せ
- 1検体 = 1ページ（`page-break-after: always`）
- 受入した検体すべてが対象。10検体受け入れたら **A4 10枚**
- 上も下もそれぞれ1枚ずつ刷る

### 刷る文字

```
畜産研究所　A1-1　上△
畜産研究所　A1-1　下▼
```

- **上 = 白抜きの△、下 = 黒塗りの▼。** ぱっと見で上下を判断できるように記号を大きく出す

### 履歴
**日報シートに「ラベル印刷」列を1つ足し、印刷した行に日時を書く。**
- 日報シートは lab-kanri が書くシートなので所有権の問題なし
- 行の特定は日報シートの行番号。`appendRow` しかしないので途中で番号がずれない

---

## 3. 機能B：揃いラベル（分析検体・複数面付け）

### 入口
「検体」ボタン → 新着揃いパネルから印刷。

### 1ラベルの寸法
**25mm × 40mm の縦長。** A4 に **7列 × 6行 = 42枚**。

### 中身（5段・上から順）

```
［2次元コード］    ← いちばん上
畜産研究           ← 件名 (現場名を4文字に切る)
■ 青              ← 色。帯にして実際の色で塗る (大きく)
A(1,2,3,4,5)      ← 区画名
BQYR-L-0006       ← コード番号
```

- **色はラベル幅いっぱいの帯**にして実際の色で塗り、中に色名を白抜きで入れる
  （赤 `#e53935` / 青 `#1e88e5` / 黒 `#000000`）
- QR は同梱ライブラリで生成。**QR の中身には `-roka` / `-huri` を含める**
- **コード番号の表示は `-roka` / `-huri` を省く**（幅が足りないため）。
  ろか用と振り用は別々に刷るので、どちらを刷ったかで区別できる

### ろか / 振り
両方印刷か片方かを選べる。**既定は「ろ過」のみON。**

### データの出どころ
`getKentaiData` が分析検体シートの A〜P をそのまま返しているので、
**L コードは既にクライアント側にある。GAS の追加実装は不要の見込み。**

---

## 4. 共通の作り

- 印刷は v2.54 の `AndroidBarcodeScan.printPage()` を使う（`window.open` は使わない）
- 印刷用の DOM を本体ページ内に組み立て、`@media print` で他を隠す（日報印刷と同じ方式）
- 印刷用クラスは印刷画面を閉じるまで外さない（v2.54 で確認済みの注意点）
- QR ライブラリを `assets/` に同梱 → オフラインで出る・コードが外部に出ない

---

## 5. GAS 側に足すもの

| 追加 | 内容 |
|---|---|
| `getLabelSiteName` / `setLabelSiteName` | 現場名の記憶（スクリプトプロパティ） |
| 日報シートに「ラベル印刷」列 | `logToDailyReport` のヘッダーに1列追加 |
| `markLabelPrinted` | 選んだ行に印刷日時を書く |
| `getDailyReportData` | 「ラベル印刷」列も返す（既に印刷済みか画面で分かるように） |
