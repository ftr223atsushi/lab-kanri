# picker への依頼：ラボ用の列を生成してほしい

2026-09-09 決定・lab-kanri 側は v2.65 で対応済み。実現場で書き込み確認済み。
**picker 側は GAS v2.12 で対応済み（2026-09-10。picker のデプロイ待ち）→ 末尾「picker側の回答」参照**

---

## 背景

shast（現場）と lab-kanri（ラボ）が同じ列を書いていて、どちらが書いたか分からなくなっていた。
**シートを分けるのではなく、列で書き手を分ける**方針にした（1列1書き手）。

とくに「採取日時」は shast が書く**作業した事実**であって、検体を採った日時ではない。
検体を現地で確認した記録は別に要るので、**現地確認**の列を新設した。

lab-kanri は**見出し名で列を引く**ようにしたので、**列の位置はどこでもよい**。
ただし見出し名は下記のとおりでないと見つけられない。

---

## 1. 実データタブに2列追加

対象: **表層土壌 / 土壌ガス / 配管・ピット・盛り土下**（深度調査は保留）

| 見出し | 書き手 |
|---|---|
| `現地確認日時` | lab-kanri |
| `現地確認担当` | lab-kanri |

既存の `採取日時` / `採取担当` は **shast のまま。触らない。**

## 2. 分析検体タブに6列追加

| 見出し | 書き手 |
|---|---|
| `振り日時` / `振り担当` | lab-kanri |
| `ろか日時` / `ろか担当` | lab-kanri |
| `分析日時` / `分析担当` | lab-kanri |

混合すると複数地点が1検体になるので、地点単位の実データタブには収まらない。
**分析検体タブの1行がその検体そのもの**なので、ここが正本。

## 3. 振りコード列を削除

会社決定で、**ろかコード1本をラボ内で使い回す**（容器が変わるときはシールを貼り替える）。
振りとろかの2枚を作って照合する運用はやめた。

- **振りコード列は不要。ろかコード列だけ残す**
- lab-kanri 側も、接尾辞で工程を判定するのをやめた（モードボタンで決める）

## 4. ガスは削孔・採取を lab-kanri から書かない

土壌ガスの `削孔日時` / `採取日時` は **shast の記録**。lab-kanri の工程は
**現地確認 / 受入 / 分析** の3つだけ。ガスはLコードを持たず、**Gコードを最後まで使う**。

---

## picker が守ること

1. **見出し名を変えない**（下記の対応表のいずれか。表記ゆれは限定的にしか許容していない）
2. **lab-kanri が書く列を picker から書かない・消さない**
3. **コードと行の対応を崩さない** ← ここが本質

### 3 について（2026-09-09 訂正）

当初「シートを作り直さない」と書いたが、地点変更のたびに**マージ差分で作り直す**運用なので
それは無理。守るべきは「作り直さない」ことではなく、**コードと行の対応が崩れないこと**。

lab-kanri はコードで行を探して書くので、記録はその行に乗っている。
だから次のどちらかが成り立っていればよい。

**方法A：行を動かさない（差分マージ）**
- 既存コードの行はそのまま残す（行ごと上下に動くのは可）
- 追加は行の挿入、削除は行の削除（または状態列を「削除」に）
- **コード列だけ書き換えて他の列はそのまま、という操作をしない**

**方法B：作り直すなら、ラボ列をコードで退避して書き戻す**
1. 作り直す前に `コード → ラボ列の値` を読んで持っておく
2. シートを作り直す
3. コードで突き合わせて、ラボ列の値を書き戻す

方法Bなら全面的に作り直しても安全。**やってはいけないのは、行の位置を前提に
ラボ列だけ残して他を入れ替えること**（コードと記録がずれる）。

対象のラボ列: 実データタブの `現地確認日時` `現地確認担当`、
分析検体タブの `振り日時` `振り担当` `ろか日時` `ろか担当` `分析日時` `分析担当`

## lab-kanri が受け付ける見出し名

実データタブ:

```
状態 / 地点名(または 地点) / 上下 / 深度(または 採取深度) / コード
削孔日時 / 削孔担当
採取日時 / 採取担当                    ← shast
現地確認日時 / 現地確認担当              ← lab-kanri (新規)
受入日時(または 受け入れ日時) / 受入担当
風乾日時 / 風乾担当
分析日時 / 分析担当                    ← ガスのみ
現地深度                              ← 配管
```

分析検体タブ:

```
区画 / 1〜9 / 黒 / 赤 / 青 / 種別 / 検体揃い状況 / 印刷日
ろかコード                            ← 振りコードは廃止
振り日時 / 振り担当                    ← lab-kanri (新規)
ろか日時(または ろ過日時) / ろか担当      ← lab-kanri (新規)
分析日時 / 分析担当                    ← lab-kanri (新規)
```

見出しが見つからない場合、lab-kanri は**従来の位置に落として動く**ので、
列を足す前の現場も止まらない。ただし新しい列は見出しが無いと使えない。

---

## picker側の回答（2026-09-10・GAS v2.12 実装済み）

§1〜4 すべて対応した。新規ブックの列構成（見出し名は受理リスト準拠）：

```
表層土壌:   状態|地点|上下|コード|採取日時|採取担当|現地確認日時|現地確認担当|受入日時|受入担当|風乾日時|風乾担当|被覆|検測深度
土壌ガス:   状態|地点名|コード|削孔日時|削孔担当|採取日時|採取担当|現地確認日時|現地確認担当|受入日時|受入担当|分析日時|分析担当|掘削方法|天候|採取方法|被覆|備考
配管等:     状態|地点|採取深度|コード|採取日時|採取担当|現地確認日時|現地確認担当|受入日時|受入担当|風乾日時|風乾担当|現地深度|被覆|検測
深度調査:   状態|地点|深度|コード|採取日時|採取担当|現地確認日時|現地確認担当|受入日時|受入担当|風乾日時|風乾担当|被覆|検測
分析検体:   区画|1〜9|黒|赤|青|種別|検体揃い状況|印刷日|風乾コード|ろかコード|振り日時|振り担当|ろか日時|ろか担当|分析日時|分析担当
深度第2種3種: 地点|深度|黒|赤|青|コード|検体揃い状況|印刷日|風乾コード|ろかコード|＋ラボ6列（分析検体と同型。地下水行の風乾コードは空）
深度第1種:  地点|深度|コード|検体揃い状況|印刷日|VOC日時|VOC担当|分析日時|分析担当
```

picker実装時の決定（ユーザー確認済み）と lab-kanri への連絡事項：

1. **風乾は残す**：風乾日時/風乾担当を土の3工種（表層・配管・深度）に配置（ガスは無し）。
   ※畜産の実物は表層から風乾列が消えているが、新規ブックには入れる方針
2. **風乾コード `-fukan` を新設**（廃止した振りコード -huri の代役。風乾工程のラベル用）。
   分析検体タブの「ろかコード」の左に「風乾コード」列がある。
   **受理リストに無い新見出しなので、lab-kanri側で扱いを決めてほしい**
3. **深度は先取り**：深度調査タブ・深度の分析検体2シートも上記の同型で生成する
   （未運用の今なら再構成不要のため。深度第1種の VOC日時/VOC担当 は仮の見出し名＝変更可）
4. **ガスの野帳5列**（掘削方法/天候/採取方法/被覆/備考＝shastの列）は新規ブックで最初から生成
5. **マージはレイアウト非依存**：追記は状態〜コード列だけ書く。既存行の他列（lab/shastの列）には
   一切触れない＝方法A準拠。畜産のような「表層=新・配管=旧」の混在ブックでも安全
6. **方法Bを実装**：分析検体・深度2シートは毎マージで作り直すため、ラボ6列を
   ろかコード（深度第1種はコード）で退避→書き戻す。**「ろかコード」列の値と見出し名が
   復元キーなので変えないこと**（印刷日は従来どおり退避対象外＝マージで消える）
7. 旧「ラボ記録」シート方式（SPEC_sheet_ownership.md §3.2）は**実装を取り下げた**。本仕様が正
