# picker への依頼：ラボ用の列を生成してほしい

2026-09-09 決定・lab-kanri 側は v2.65 で対応済み。実現場で書き込み確認済み。

---

## 背景

shast（現場）と lab-kanri（ラボ）が同じ列を書いていて、どちらが書いたか分からなくなっていた。
**シートを分けるのではなく、列で書き手を分ける**方針にした（1列1書き手）。

とくに「採取日時」は shast が書く**作業した事実**であって、検体を採った日時ではない。
検体を現地で確認した記録は別に要るので、**現地確認**の列を新設した。

lab-kanri は**見出し名で列を引く**ようにしたので、**列の位置はどこでもよい**。
ただし見出し名は下記のとおりでないと見つけられない。

---

## 1. 実データタブに2列追加

対象: **表層土壌 / 土壌ガス / 配管・ピット・盛り土下**（深度調査は保留）

| 見出し | 書き手 |
|---|---|
| `現地確認日時` | lab-kanri |
| `現地確認担当` | lab-kanri |

既存の `採取日時` / `採取担当` は **shast のまま。触らない。**

## 2. 分析検体タブに6列追加

| 見出し | 書き手 |
|---|---|
| `振り日時` / `振り担当` | lab-kanri |
| `ろか日時` / `ろか担当` | lab-kanri |
| `分析日時` / `分析担当` | lab-kanri |

混合すると複数地点が1検体になるので、地点単位の実データタブには収まらない。
**分析検体タブの1行がその検体そのもの**なので、ここが正本。

## 3. 振りコード列を削除

会社決定で、**ろかコード1本をラボ内で使い回す**（容器が変わるときはシールを貼り替える）。
振りとろかの2枚を作って照合する運用はやめた。

- **振りコード列は不要。ろかコード列だけ残す**
- lab-kanri 側も、接尾辞で工程を判定するのをやめた（モードボタンで決める）

## 4. ガスは削孔・採取を lab-kanri から書かない

土壌ガスの `削孔日時` / `採取日時` は **shast の記録**。lab-kanri の工程は
**現地確認 / 受入 / 分析** の3つだけ。ガスはLコードを持たず、**Gコードを最後まで使う**。

---

## picker が守ること

1. **見出し名を変えない**（下記の対応表のいずれか。表記ゆれは限定的にしか許容していない）
2. **lab-kanri が書く列を picker から書かない・消さない**
3. **コードと行の対応を崩さない** ← ここが本質

### 3 について（2026-09-09 訂正）

当初「シートを作り直さない」と書いたが、地点変更のたびに**マージ差分で作り直す**運用なので
それは無理。守るべきは「作り直さない」ことではなく、**コードと行の対応が崩れないこと**。

lab-kanri はコードで行を探して書くので、記録はその行に乗っている。
だから次のどちらかが成り立っていればよい。

**方法A：行を動かさない（差分マージ）**
- 既存コードの行はそのまま残す（行ごと上下に動くのは可）
- 追加は行の挿入、削除は行の削除（または状態列を「削除」に）
- **コード列だけ書き換えて他の列はそのまま、という操作をしない**

**方法B：作り直すなら、ラボ列をコードで退避して書き戻す**
1. 作り直す前に `コード → ラボ列の値` を読んで持っておく
2. シートを作り直す
3. コードで突き合わせて、ラボ列の値を書き戻す

方法Bなら全面的に作り直しても安全。**やってはいけないのは、行の位置を前提に
ラボ列だけ残して他を入れ替えること**（コードと記録がずれる）。

対象のラボ列: 実データタブの `現地確認日時` `現地確認担当`、
分析検体タブの `振り日時` `振り担当` `ろか日時` `ろか担当` `分析日時` `分析担当`

## lab-kanri が受け付ける見出し名

実データタブ:

```
状態 / 地点名(または 地点) / 上下 / 深度(または 採取深度) / コード
削孔日時 / 削孔担当
採取日時 / 採取担当                    ← shast
現地確認日時 / 現地確認担当              ← lab-kanri (新規)
受入日時(または 受け入れ日時) / 受入担当
風乾日時 / 風乾担当
分析日時 / 分析担当                    ← ガスのみ
現地深度                              ← 配管
```

分析検体タブ:

```
区画 / 1〜9 / 黒 / 赤 / 青 / 種別 / 検体揃い状況 / 印刷日
ろかコード                            ← 振りコードは廃止
振り日時 / 振り担当                    ← lab-kanri (新規)
ろか日時(または ろ過日時) / ろか担当      ← lab-kanri (新規)
分析日時 / 分析担当                    ← lab-kanri (新規)
```

見出しが見つからない場合、lab-kanri は**従来の位置に落として動く**ので、
列を足す前の現場も止まらない。ただし新しい列は見出しが無いと使えない。
